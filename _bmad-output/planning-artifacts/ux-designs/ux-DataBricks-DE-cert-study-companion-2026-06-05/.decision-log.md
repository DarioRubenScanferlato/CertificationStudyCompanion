# UX Decision Log — DataBricks DE Cert Study Companion

Canonical memory and audit trail for the UX workflow. Every decision, creative-tool artifact, import, and override is recorded here as the conversation unfolds. The spines (DESIGN.md, EXPERIENCE.md) win on conflict.

| # | Date | Decision | Rationale / Source |
|---|------|----------|--------------------|
| 1 | 2026-06-05 | Intent = **Create** (no prior DESIGN.md/EXPERIENCE.md). UX **follows** an existing MVP frontend (React 3-screen app: SessionSelect → MCQPractice → Summary). | Workspace scan; user request "now that we have a basic MVP, let's create a better UX". |
| 2 | 2026-06-05 | Scope = **functional / quality-of-life behaviors, not visual redesign**. DESIGN.md stays minimal (inherit current Tailwind + databricks palette); weight goes to EXPERIENCE.md. | User: "no need to focus so much on design." |
| 3 | 2026-06-05 | **Session control** (all selected): always-visible End-session→home; confirm before discarding; clickable header = home; Restart-same-session from Summary. | User selection. Closes the stated gap: no mid-session exit today (`reset` only on Summary). |
| 4 | 2026-06-05 | **Navigation & input** (all selected): visual progress bar + running score; Back/Previous (answered questions read-only); keyboard shortcuts (1–4 / a–d select, Enter submit→advance); Skip (counts as unanswered). | User selection. |
| 5 | 2026-06-05 | **Setup & review** (subset): match-count before start; review-incorrect at end (+ practice-those-again); quit-to-partial-summary. **Not** selected: choose-session-length (always full filtered set for now). | User selection; session-length deferred. |
| 6 | 2026-06-05 | Practice screen specced **single-select only** (per PRD rev 2 / Epic 5); EXPERIENCE.md does not spec multi-select affordances. | Consistency with PRD rev 2; current code's multi-select path is removed in Epic 5. |
| 7 | 2026-06-05 | EXPERIENCE.md (status draft) written: IA, voice/tone, component patterns, state patterns, interaction primitives (keyboard), accessibility floor, 3 key flows covering the QoL set. DESIGN.md kept minimal (inherits implemented databricks palette). | User-selected QoL scope (decisions #3–5). |
| 8 | 2026-06-05 | **Architecture rev 3** applied (all 3 gaps): G1 `GET /api/exercises/count` (leak-free match count); G2 `POST /api/sessions {exerciseIds}` (replay keeps FR-20/21 freshness); G3 frontend feedback-retention + reducer actions (`prev`, `skip`, `endToSummary`, replay) and per-question/session states. | User chose "Apply all 3 to architecture.md". G1/G2 protect the FR-20 non-leakage rule; G2 preserves replay freshness. |
| 9 | 2026-06-05 | **Reviewer gate skipped** by user choice (no rubric/accessibility/consistency subagents run). Self-triage only. | User selected "Skip validation" at the lens menu. |
| 10 | 2026-06-05 | Open items triaged: replay-resample NOTE **resolved** by G2; two assumptions (skip-revisit, full-length session) **accepted**; all surfaces **spine-only** (no mocks, per functional-pass scope). | Finalize triage. |
| 11 | 2026-06-05 | **UX FINALIZED** — DESIGN.md + EXPERIENCE.md set `status: final`. Formal editorial doc-standards skills not run (light self-polish; lean/hobby stakes). | Finalize complete. |

## Sources (by reference — not duplicated into spines)
- PRD rev 2: `_bmad-output/planning-artifacts/prds/prd-DataBricks-DE-cert-study-companion-2026-06-05/prd.md`
- Addendum: same folder `addendum.md`
- Architecture rev 2: `_bmad-output/planning-artifacts/architecture.md`
- Epics: `_bmad-output/planning-artifacts/epics.md`
- Current-state frontend: `frontend/src/` (pages: SessionSelect, MCQPractice, Summary; components: QuestionContent, CodeBlock)
