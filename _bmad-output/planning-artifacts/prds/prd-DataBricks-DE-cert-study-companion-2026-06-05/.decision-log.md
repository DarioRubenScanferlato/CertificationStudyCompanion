# Decision Log — Databricks DE Cert Study Companion PRD

Canonical memory and audit trail. Every decision, change, and override is recorded here.

| # | Date | Decision | Rationale / Source |
|---|------|----------|--------------------|
| 1 | 2026-06-05 | Intent = **Create** (no existing PRD). | Fresh scaffold; no prior runs to resume. |
| 2 | 2026-06-05 | Stakes = **"Me now, maybe share later"** (medium rigor). | User selection. Drives lean-but-extensible PRD. |
| 3 | 2026-06-05 | Working mode = **Fast path**. | User selection. Draft full PRD with `[ASSUMPTION]` tags, then iterate. |
| 4 | 2026-06-05 | Target cert = **Both, Associate first**. | User selection. Taxonomy spans both certs; v1 content seeds Associate. |
| 5 | 2026-06-05 | Exercise source = **Author via agent skill** into a standardized parseable format; in-app generation deferred/optional. | User selection. Simplest path to MVP; matches the "exam in 1-2 months" timeline. |
| 6 | 2026-06-05 | Study features: **answer explanations in MVP**; timed mock-exam, weak-area analytics, spaced repetition deferred to later phases. | User selection. |
| 7 | 2026-06-05 | Timeline = **exam within ~1-2 months**. | User selection. MCQ MVP must be usable fast; nice-to-haves explicitly phased. |
| 8 | 2026-06-05 | Tech stack (React vs HTMX; Rust vs Python+uv) **deferred to architecture**; captured in addendum, not PRD. | User brief; PRD stays capability-focused. |
| 9 | 2026-06-05 | Wordle-code design follows research: **token-level feedback, single-line/fill-in-blank scope, whitespace-insensitive, multi-answer validation**; literal per-char + "yellow" feedback flagged as risky. | Comparables research digest (2026-06-05). Captured as assumptions for confirmation. |
| 10 | 2026-06-05 | Live exam facts (question counts, weights, price, validity, DLT→Lakeflow rename) **must be re-verified** against current Databricks PDF exam guides before content authoring. | Cert research ran without live network; grounded in model knowledge. Logged as Open Question. |
| 11 | 2026-06-05 | Code-Completion Positional Feedback = **green/yellow/grey at the TOKEN level** (CONFIRMED). Resolves OQ-3's color-model question. | User decision after reviewing the yellow-mechanic trade-off. Yellow retained (token present, wrong slot); per-character feedback rejected as misleading for code. Design spike now covers tokenizer + rendering only. |
| 12 | 2026-06-05 | **Build-vs-Borrow:** borrow/go-lean for MCQ study now (Anki via FR-18 or a lean built-in runner); reserve from-scratch build for the novel Wordle Code-Completion feature (Phase 2). Content bank is the primary MVP deliverable; app shell is secondary. | User selection at Finalize triage, prompted by adversarial review ("content is the real product; building competes with studying for a deadline"). New §6.0. Added FR-18 (portable/exportable content), leaned FR-3. |
| 13 | 2026-06-05 | **Content bank committed** as first-class MVP deliverable: ~50–75 original Associate MCQs weighted by domain split, with a committed agent-skill authoring track. Resolves OQ-4 and OQ-5. | User selection at Finalize triage. SM-2 rewritten around the target. |
| 14 | 2026-06-05 | FR-9 multi-select scoring promoted from buried assumption to stated **all-or-nothing** decision; UJ-1 FR-range typo fixed (FR-5–12); dead `char` granularity option marked v1-unsupported. | Rubric + adversarial + reconcile findings; mechanical autofixes. |
| 15 | 2026-06-05 | OQ-1 (verify live exam facts) reclassified as a **step-zero gate** on the content critical path, not a background open question. | Adversarial finding — unresolved external dependency on critical path. |
| 17 | 2026-06-05 | OQ-1 clarified (2026-06-05): Gate is **domain list + weights only**. MCQs authored to current official docs (Lakeflow Pipelines, etc.), not reverse-engineered from real exam questions. Question counts/passing score/price are nice-to-know, not blocking. | User clarification via direct chat. Reduces scope/urgency of OQ-1; it's a prioritization gate, not a validation gate. |

| 16 | 2026-06-05 | **PRD finalized** (`status: final`). Reviewer gate passed (rubric: strong/build-ready, 0 critical/0 high; adversarial findings triaged into the Build-vs-Borrow reframe; brief reconciliation clean). Editorial polish applied. | Finalize complete. |

## Update — Option Pool & Randomization (2026-06-05, revision 2)

| # | Date | Decision | Rationale / Source |
|---|------|----------|--------------------|
| 18 | 2026-06-05 | **MCQ Option Pool** (new FR-19): each MCQ authored with **≥1 correct and ≥3 incorrect** Options, no upper bound; extra correct alternatives and/or distractors encouraged. | User update request: a richer pool lets the runner show a different combination each view, so a question feels new on re-study. Aligns PRD with the `write-mcq` agent skill's option rules. |
| 19 | 2026-06-05 | **MCQ is single-select only; multi-select variant REMOVED.** Each display = exactly 1 correct + 3 distractors sampled from the pool, **positions shuffled** (new FR-20). FR-7 revised to single-select; FR-9 (all-or-nothing multi-select scoring) tombstoned; FR-12/UJ-1/Glossary updated. | User decision (AskUserQuestion 2026-06-05): "Single-select only" + "shuffle positions." Supersedes decision #14's multi-select scoring. Pool's multiple correct Options are interchangeable alternatives, not jointly-required sets. |
| 20 | 2026-06-05 | **Randomized Exercise order per Practice Session** (new FR-21): order re-randomized each session; no SRS weighting in MVP. | User update request: avoid encountering the same questions in the same order every session start. |
| 21 | 2026-06-05 | **Content-migration follow-up flagged:** existing authored "select all that apply" items (jointly-correct sets) must be reworked or excluded under the single-select decision; validation should flag non-interchangeable correct Options. | Consequence of #19. Logged as `[NOTE FOR PM]` in FR-9. Non-blocking; deferred to content/validation work. |
| 22 | 2026-06-05 | **Anki-export caveat:** FR-20/FR-21 are runner-only behaviors; the one-shot Anki export flattens each pool to a single correct Option + distractors and relies on Anki's own shuffle/scheduling. Full Option-Pool freshness is realized only in the built-in runner. | Noted in §6.1 to keep the Build-vs-Borrow study path honest. |

## Update — Scope expansion: Stats Tracking + Timed Practice (2026-06-07, revision 3)

| # | Date | Decision | Rationale / Source |
|---|------|----------|--------------------|
| 23 | 2026-06-07 | **Scope expanded** post-MVP: promote two §6.2-deferred phases to active scope — **Answer & Stats Tracking** (§4.5, FR-22–FR-25 → Epic 7) and **Timed Practice / Mock Exam** (§4.6, FR-26–FR-28 → Epic 8). Code-Completion (Epic 4) stays deferred. | User: "expand scope now that we have a working MVP; biggest gaps are stats tracking and timer." |
| 24 | 2026-06-07 | **Persistence reversed → local SQLite** (Python stdlib, no pip). Reverses the no-persistence/stateless-session stance; single-user/local retained. JSON file considered, rejected for the analytics/SRS horizon. | User selection (AskUserQuestion). Mechanism in addendum §E. |
| 25 | 2026-06-07 | **Stats scope = all four**: attempt history, stats dashboard, unseen-first prioritization, readiness indicator. **SRS still deferred.** FR-24 unseen-first supersedes FR-21's uniform-random *ordering* assumption (sampling stays random). | User selection (multi-select). |
| 26 | 2026-06-07 | **Timer = both**: lightweight optional session countdown (FR-26) AND full Mock-Exam mode (FR-27, domain-weighted full-length + real exam timing + exam-style score); per-question timing (FR-28) feeds stats. | User selection. |
| 27 | 2026-06-07 | **Process = full BMad flow**: PRD update (this) → architecture rev 4 (SQLite persistence, unseen-first policy, mock-exam builder) → epics (7, 8) + stories → dev. | User selection. |

## Update — Code-Completion promoted to active scope (2026-06-08, revision 4)

| # | Date | Decision | Rationale / Source |
|---|------|----------|--------------------|
| 28 | 2026-06-08 | **Code-Completion (§4.3, FR-13–FR-17, Epic 4) promoted from Phase-2/§6.2-deferred to active scope** — the final epic, after Epics 1–8 shipped. Added §6.4. | User: "the code completion exercise is now in scope, realign prd and architecture." |
| 29 | 2026-06-08 | **§4.3 design stance flipped ASSUMPTION → CONFIRMED:** single-line/fill-in-blank, whitespace-insensitive (`ignore_whitespace`), per-language case policy (`case_sensitive`), token-level green/yellow/grey, authored `accepted` alternatives — all locked in the shipped `CodeCompletion` model + Epic 4 stories. OQ-3 fully resolved; throwaway-spike recommendation superseded. | The model exists; stories 4.1–4.5 already authored at FR depth. |
| 30 | 2026-06-08 | **Feedback is CLIENT-SIDE; `POST /api/feedback/code-completion` NOT implemented.** Resolves a stale architecture-doc reference contradicting NFR-1 (< 100ms, client-side — the React-choice rationale). Consequence: the active exercise's answer + accepted alternatives are delivered to the client — a deliberate trade-off (Wordle loop reveals the answer anyway; no MCQ gaming surface) that does NOT relax MCQ FR-20 non-leakage. | User selection (AskUserQuestion 2026-06-08): "Client-side, record trade-off." Mechanism in addendum §F. |
| 31 | 2026-06-08 | **Story coverage confirmed + one addition:** stories 4.1–4.5 cover FR-13–17 + NFR-1 fully; **no new FRs needed.** Added **Story 4.6 — Code-Completion content + `write-code-completion` skill** to close the content gap (no code-completion content exists; `write-mcq` is MCQ-only), mirroring the committed MCQ authoring track (§6.1). Makes SM-4 achievable. | User selection (AskUserQuestion 2026-06-08): "Add Story 4.6 (content + skill)." |
| 32 | 2026-06-08 | **Code-Completion attempts NOT recorded** to the MCQ-scoped SQLite store; no stats/readiness/per-question-timing for code-completion. Logged as a known gap (future story), not built. | Consequence of client-side feedback + MCQ-scoped Epic 7 store. |
| 33 | 2026-06-08 | **Next: architecture rev 5** must remove the `code-completion` server endpoint + its component-boundary line, and add the client-side tokenizer/feedback modules + the session-builder code-completion delivery. Captured in addendum §F. | User: "we need to also realign the architecture document." → `bmad-create-architecture`. |

## Update — Generalization + Shareability pivot (2026-06-09, revision 5)

Change signal (user): "Next step is sharing this app with colleagues. We need to be able to run the app with a simple docker compose. We also need to begin changing the app from databricks focused to generic, so that courses from other cloud providers can be added. We don't want to lose info on databricks, but the titles should reflect a companion that can be applied to any IT certification."

| # | Date | Decision | Rationale / Source |
|---|------|----------|--------------------|
| 34 | 2026-06-09 | **Product renamed → "Cert Study Companion"** (generic, provider-agnostic working title). Databricks DE Associate/Professional become the **first bundled certification(s)**, fully intact. | User: titles should reflect a companion applicable to any IT certification. AskUserQuestion: "New product name, keep paths." All document titles/README/UI retitle; **folder names, BMad `project_name`, and artifact paths are left unchanged** to avoid breaking artifacts/git history. |
| 35 | 2026-06-09 | **Sharing model = each colleague runs their OWN instance via `docker compose up`.** Single-user **per instance**; each person's SQLite history stays local (mounted volume). **No accounts/auth/shared DB/sync.** | User: "share with colleagues … run with a simple docker compose." AskUserQuestion: "Each runs their own (Recommended)." Smallest reversal of the single-user/local stance — packaging change, not a hosted multi-user service. The "not hosted multi-user" non-goal is retained; the "no containerization" stance is reversed. |
| 36 | 2026-06-09 | **Data model + vocabulary generalized to be provider-agnostic** (reframe, keep DBX as seed). New glossary: **Provider** (vendor/cloud) and **Certification** (a specific cert exam offered by a Provider). The existing `exam` field is **retained as the Certification identifier** (semantics generalized; Associate/Professional become two Certifications under the Databricks Provider) so the Story 6.7 exam filter keeps working. Domain lists/weights/exam params become **per-Certification config**, not hardcoded Databricks. Code-Completion target language stays **per-exercise** (already is) rather than Spark-only. | User: "change the app from databricks focused to generic, so that courses from other cloud providers can be added." AskUserQuestion: "Reframe structure + keep DBX as seed (Recommended)." Avoids a deep code rewrite while unblocking other providers. |
| 37 | 2026-06-09 | **New FRs:** FR-29 (per-Certification configuration — adding a certification is content + config, no code change), FR-30 (multi-Certification content organization + Provider/Certification selection; generalizes the Associate/Professional exam filter), FR-31 (one-command containerized run via `docker compose up` for local sharing). **New NFR-5** (shareability: runs with no host toolchain — no local Node/Python install needed). | Derived from decisions 34–36. Kept capability-level; mechanism (compose file, volume, config schema) → addendum §G/§H. |
| 38 | 2026-06-09 | **Non-Goals updated:** "Not multi-cert beyond Databricks DE" is **reversed** — multi-certification is now the explicit direction (FR-29/30). "Not a hosted, multi-user service" is **retained** (sharing is per-own-instance, not hosting). "No containerization" is **reversed** (docker compose is now the share path). | Consequence of 35/36. |
| 39 | 2026-06-09 | **Full BMad flow:** PRD rev 5 (this) → architecture rev 6 (per-Certification config loader + `exam`→certification semantics; Dockerfiles + docker-compose.yml + SQLite volume; mock-exam params per-Certification) → epics (new Epic 9 Generalization, Epic 10 Containerization & Sharing) + stories → dev. | User: "update the prd, architecture, and epics." |
| 40 | 2026-06-09 | **Scope sequencing:** generalization + containerization is the **current active iteration**, layered on the shipped Epics 1–8 + Epic 4. Deep generalization extras (provider/cert **switcher UI**, bundling a *second* provider's real content, per-provider blueprint validation) are **deferred** — this iteration delivers the provider-agnostic model + config + one bundled provider (Databricks) + docker compose. | AskUserQuestion declined "Full generalization now" in favor of "Reframe structure + keep DBX as seed." |

## Update — Epic 4 implemented + Story 4.7 + code-review (2026-06-09 → 2026-06-10)

Implementation + review of the Code-Completion runner and the exercise-type filter (dev-story + code-review runs).

| # | Date | Decision | Rationale / Source |
|---|------|----------|--------------------|
| 41 | 2026-06-09 | **Epic 4 Code-Completion runner implemented** (stories 4.1–4.5): client-side `tokenizer.js` + `codeFeedback.js` (two-pass Wordle, accepted alternatives), `FeedbackTokens.jsx` (color-independent), guess loop in `CodeCompletion.jsx` (bounded attempts via `CODE_COMPLETION_MAX_ATTEMPTS=6`, reveal + explanation, Next). No server endpoint; feedback is client-side (NFR-1). Stories → `review`. | dev-story run. The drill was previously only a Story-4.1 display shell (`handleGuess` was a no-op). |
| 42 | 2026-06-09 | **New Story 4.7 — Exercise-Type Filter (multiselect, MCQ default).** Backend `exercise_type` generalized to multi-value (any-of) on `GET /api/sessions` + `/api/exercises/count`; Start screen gains a checkbox multiselect, MCQ checked by default. Closes the discoverability gap (code-completion was unreachable in the UI). | User: "make sure we have a story for the filter on exercise type (multiselect with mcq selected by default)." Supersedes the single-select dropdown built earlier the same day. |
| 43 | 2026-06-10 | **Code-review fixes (10 findings) applied** before merge. Notable architectural decisions below (44–46). All correctness findings fixed; suites green (frontend 182, backend 269), lint clean. Stories stay `review` pending human approval. | `/code-review` (high effort, 7 finder angles). Findings clustered on *mixed MCQ + code-completion sessions* (now reachable via the multiselect). |
| 44 | 2026-06-10 | **In-session Code-Completion result retention** added to `SessionContext` (`codeCompletionResults` map). A concluded drill survives unmount so Back/Next shows it final (parity with MCQ `feedback` retention). This is **session-scoped client state only** — it does NOT reverse decision #32 (code-completion is still never written to the MCQ-scoped SQLite store; no stats/readiness/timing). | Review finding #1 (Back-nav wiped a solved drill and re-armed it). |
| 45 | 2026-06-10 | **Unseen-first ordering excludes Code-Completion.** Since code-completion is never recorded it would otherwise be permanently "unseen" and lead every session. `_order_unseen_first` now applies unseen-first to MCQs only and sprinkles code-completion at random positions. | Review finding #4. Refines AR-17 / FR-24 ordering for the mixed-type case. |
| 46 | 2026-06-10 | **Shared runner shell extracted** to remove MCQ/Code-Completion duplication: `styles/ui.js` (FOCUS_RING/FOCUS_RING_NEUTRAL/DIFFICULTY_STYLES), `components/ExplanationPanel.jsx`, `hooks/useSessionExit.js`, `utils/language.js` (single `LANGUAGE_ALIASES`). `computeFeedback` returns `{tokens, solved}` (no monkey-patched array property). Summary excludes code-completion from MCQ tallies and shows a separate "Code drills: X/Y solved" line. | Review findings #3, #7, #8, #9, #10. |

**Still open (not in the fixed set):** Story 4.6 (starter code-completion content bank + `write-code-completion` skill) remains `ready-for-dev`; honorable-mention nits (dead `'OR REPLACE'` keyword, tokenizer regex recompile, `_validate_exercise_types` vs scalar-loop duplication) left as-is.

## Update — Question feedback & content-improvement loop (2026-06-10, PRD revision 6)

Change signal (user): "add functional requirement - I should be able to add feedback to the question through the frontend. When I do so, it should be saved in the yaml. The skill to create mcq should allow editing bad questions by taking into account the feedback provided." (Then: create a story and develop.)

| # | Date | Decision | Rationale / Source |
|---|------|----------|--------------------|
| 47 | 2026-06-10 | **New feature §4.9 — Question Feedback & Content-Improvement Loop** (FR-32 capture in-app feedback → persisted; FR-33 `write-mcq` revises flagged questions from feedback + marks resolved). New **Epic 11**. | User change signal. Turns study friction into a better content bank. |
| 48 | 2026-06-10 | **Feedback stored in a SIDECAR YAML** (e.g. `exercises/feedback.yaml`, keyed by Exercise `id`), NOT inline in the authored Exercise files — which stay byte-unchanged. | AskUserQuestion: "Sidecar feedback file (Recommended)." Avoids clobbering hand-authored YAML comments/Option-Pool layout; clean for the skill to read. |
| 49 | 2026-06-10 | **Feedback shape = free-text note** (+ server-stamped timestamp + `resolved` flag). No category/severity. | AskUserQuestion: "Free-text note only." Keeps the capture UI minimal. |
| 50 | 2026-06-10 | **`write-mcq` revises in place + marks resolved.** The skill reads open feedback, edits the Exercise's source YAML to fix the issue, re-validates, and marks the entry resolved; author reviews the diff (no approval UI). MCQ-first; code-completion revision is a later extension. | AskUserQuestion: "Revise in place + mark resolved (Recommended)." |
| 51 | 2026-06-10 | **Narrowed the "Not a content authoring UI" non-goal (§5).** The app gains its FIRST content-write path (feedback notes → sidecar) but still does NOT edit questions/options/explanations/config in-app. Distinct from the SQLite attempt store (stays MCQ-scoped). | Consequence of #47/#48. Recorded as a deliberate, narrow reversal. |
| 52 | 2026-06-10 | **OQ-9 opened:** is `feedback.yaml` committed (travels with content; single-author) or local-per-instance (like `progress.db`)? Default leaning committed; decide in Epic 11. Also reinforces OQ-8 (writeback needs a writable content bind-mount, not a baked read-only image). | Consequence of the sidecar + docker sharing model. |
| 53 | 2026-06-10 | **Next:** create Story 11.1 (in-app feedback capture + sidecar persistence — backend endpoint + `feedback_store.py` + frontend affordance) for immediate development; Story 11.2 (`write-mcq` feedback-driven revision) follows. Architecture impact in addendum §I (→ architecture rev 8). | User: "create a story and then we'll develop it." |

## Update — Code-Completion feedback reversal + Skip (2026-06-10, correct-course)

Change signal (user): "the code completion exercise experience is quite unpleasant. I think we should be having checks per letter like in wordle, rather than token. Especially because code completion exercises only require one word. And there should be a button to skip, rather than forcing the user to exhaust attempts." Sprint Change Proposal: `_bmad-output/planning-artifacts/sprint-change-proposal-2026-06-10.md`.

| # | Date | Decision | Rationale / Source |
|---|------|----------|--------------------|
| 54 | 2026-06-10 | **Code-Completion feedback REVERSED: token-level → CHARACTER-level (per-letter Wordle).** Supersedes #11 and #29. `tokenizer.js` is removed (it was only used for token feedback); `codeFeedback.js` becomes a per-character two-pass engine and `FeedbackTokens.jsx` renders per-character tiles. Case follows `case_sensitive` per-character; whitespace per `ignore_whitespace`; accepted alternatives scored best-candidate. | User course-correction. Every answer is a single fill-in-the-blank word, so token-level feedback is binary (all-green/all-grey) — no guess-and-narrow. The #11 "per-char misleading for code" concern was about multi-token snippets and doesn't apply to one word. PRD §4.3/FR-14, OQ-3 revised; architecture feedback-engine + AR-9 superseded. |
| 55 | 2026-06-10 | **Skip added to the Code-Completion runner.** A Skip control advances to the next exercise **without revealing** the answer/explanation (distinct from solve/exhaustion, which reveal). The 6-guess cap (`CODE_COMPLETION_MAX_ATTEMPTS`) is retained as the auto-reveal backstop — Skip is the early bail. | User: "a button to skip, rather than forcing the user to exhaust attempts." Mode/behavior chosen via AskUserQuestion (just-advance / keep-6-cap). PRD FR-15 updated. |
| 56 | 2026-06-10 | **Implementation tracked as new Story 4.8 — Character-Level Feedback + Skip** (ready-for-dev), superseding the token-granularity parts of shipped stories 4.3/4.4 and extending 4.5. No backend change; no content change (answers already single words). | Sprint Change Proposal handoff (Moderate scope → dev-story). |

## Update — OQ-9 resolved (2026-06-11)

| # | Date | Decision | Rationale / Source |
|---|------|----------|--------------------|
| 57 | 2026-06-11 | **OQ-9 resolved: `exercises/feedback.yaml` is LOCAL-per-instance (gitignored), not committed.** Added to `.gitignore`. | User decision. Mirrors the SQLite attempt store (`backend/data/`, also gitignored): each shared instance (Epic 10 docker) keeps its own feedback, no cross-writing of a committed file. The `write-mcq` revision skill (FR-33) reads whatever is local. PRD §8 OQ-9 + §9 assumption updated. |

## Research artifacts (Discovery)
- Cert structure & technical surface digest — Databricks DE Associate (~45q/90min) & Professional (~60q/120min), domain weights, code surface. Source: subagent, model knowledge (network blocked), 2026-06-05.
- Comparables & format digest — practice-app feature table, Wordle-for-code pitfalls, MCQ/code YAML schemas. Source: subagent, model knowledge (network blocked), 2026-06-05.
